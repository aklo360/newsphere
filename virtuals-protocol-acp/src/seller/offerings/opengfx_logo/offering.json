{
  "id": "opengfx_logo",
  "name": "OpenGFX Logo System",
  "description": "Generate a complete logo system from concept. Includes icon, wordmark, stacked lockup, horizontal lockup, and brand-system.json. Brand name is optional — AI generates one if not provided.",
  "price": {
    "amount": 5,
    "currency": "USDC"
  },
  "category": "design",
  "tags": ["logo", "branding", "icon", "wordmark", "identity"],
  "inputSchema": {
    "type": "object",
    "required": ["concept"],
    "properties": {
      "concept": {
        "type": "string",
        "description": "Brand concept, vibe, industry, style direction (e.g., 'Modern fintech startup, bold and trustworthy')"
      },
      "brand_name": {
        "type": "string",
        "description": "Brand name (optional — AI generates if not provided)"
      },
      "tagline": {
        "type": "string",
        "description": "Optional tagline/slogan"
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "icon": { "type": "string", "description": "CDN URL for icon (1024x1024)" },
      "wordmark": { "type": "string", "description": "CDN URL for wordmark" },
      "stacked": { "type": "string", "description": "CDN URL for stacked lockup" },
      "horizontal": { "type": "string", "description": "CDN URL for horizontal lockup" },
      "brandSystem": { "type": "string", "description": "CDN URL for brand-system.json" }
    }
  },
  "estimatedDuration": "1-2 minutes",
  "examples": [
    {
      "input": {
        "concept": "AI-powered fitness coaching for busy professionals",
        "tagline": "Your pocket trainer"
      },
      "description": "AI generates brand name from concept"
    },
    {
      "input": {
        "brand_name": "Acme",
        "concept": "Modern fintech startup, bold and trustworthy"
      },
      "description": "Generate logo for existing brand name"
    }
  ]
}
